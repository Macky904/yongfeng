# Supabase / PostgreSQL 连接串示例（仅作格式说明，不要在此填真实密码）
# 复制本文件为 .env 后填入真实连接串，.env 已被 .gitignore 忽略，不会提交。
#
# 格式（Supabase Pooler）：
DATABASE_URL=postgresql://postgres.xxxxx:你的密码@xxxxx.pooler.supabase.com:5432/postgres?sslmode=require
#
# 也可同时指定要导出的表名（移仓换月 / symbol 子项目用）：
# PG_TABLE=public.移仓换月宽表
