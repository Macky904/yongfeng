# yongfeng 任务数据库 ↔ 代码文件索引

> 说明：本索引按 **yongfeng/ 下每个独立任务文件夹** 为单位整理。
> - **代码文件** 列包含文件夹内全部 `.py` 与 `.sql`（不再只限 Python），每个文件名后附中文翻译。
> - 已排除非代码文件：`__pycache__/*.pyc`、`data/`、`exports/`、`README.md`、`requirements.txt`、`.gitignore`、`.xlsx`/`.csv`/`.sqlite` 等数据/缓存文件。
> - 根层脚本（`update_foreign_kline_tv.py`、symbol 费用更新脚本等）按此前约定**未纳入**（它们不在任务文件夹内，未上 GitHub）。`news_articles/` 的全部脚本（含 14 个探索脚本 + `build_news_articles.py`）均已移入该文件夹并纳入本索引。

---

## 总览表

| 序号 | 工作内容（文件夹） | 数据库名 | 代码文件（含 .py/.sql，附中文名） | 流程说明 |
|---|---|---|---|---|
| 1 | news_articles | public.news_articles | 见下方明细 | 见下方明细 |
| 2 | symbol表 | public.symbol | 见下方明细 | 见下方明细 |
| 3 | 外盘期货日线数据库 | public.futures_kline_daily | 见下方明细 | 见下方明细 |
| 4 | 套期保值hedging_announcements | public.hedging_announcements | 见下方明细 | 见下方明细 |
| 5 | 移仓换月宽表 | public.移仓换月宽表 | 见下方明细 | 见下方明细 |

---

## 1. news_articles（public.news_articles）

**代码文件**
- `01_create_news_articles.sql` → 01_建表（news_articles 表，20 字段 + 触发器函数）
  - 含 4 段：①`update_updated_at_column()` 触发器依赖函数 ②`CREATE TABLE` 建表（news_no / id / title / content / ai_summary / source_url / impact_score / status / is_urgent / tags 等共 20 字段）③3 个索引（news_no、status+pub_date、topic）④`updated_at` 自动更新触发器
  - 说明：完全沿用你最初给的原始建表语句，**未额外加字段**（此前误加的 `platform` / `raw_json` 两列已删除、回归原样）
- `build_news_articles.py` → 建表并执行（连接 Postgres 执行上述建表 DDL 并校验表结构/行数；内嵌 DDL 已与 `01_create_news_articles.sql` 同步为干净版，无 platform/raw_json）

**探索脚本（`news_articles/探索脚本/`，14 个，均为测试各新闻源 API 可用性的实验脚本）**
- `desc_news.py` → 查看表结构（查看 news_articles 表字段/行数）
- `news_gdelt_probe.py` → GDELT 新闻源探测（一）
- `news_gdelt_probe2.py` → GDELT 新闻源探测（二）
- `probe_akshare_news.py` → akshare 新闻源探测（一）
- `probe_akshare_news2.py` → akshare 新闻源探测（二）
- `probe_akshare_news3.py` → akshare 新闻源探测（三）
- `probe_baidu_news.py` → 百度新闻源探测
- `probe_baidu_raw.py` → 百度新闻原始接口探测
- `probe_eastmoney_news.py` → 东方财富新闻源探测
- `probe_em_futures_news.py` → 东方财富期货新闻探测
- `probe_em_search.py` → 东方财富搜索接口探测
- `probe_gdelt_cn.py` → GDELT 中文新闻探测
- `probe_sina_news.py` → 新浪新闻源探测
- `probe_sources.py` → 多新闻源汇总探测

**流程说明**
1. `01_create_news_articles.sql` 是建表定义（也可被 `build_news_articles.py` 内嵌执行）；
2. `build_news_articles.py` 连库执行建表、并验证列结构/行数；
3. `探索脚本/` 下 14 个脚本用于验证各新闻数据源（akshare / 百度 / 东方财富 / 新浪 / GDELT 等）的可用性与字段结构，是前期选型实验，**非生产采集流水线**；
4. 建表后靠触发器自动维护 `updated_at`。

---

## 2. symbol表（public.symbol）

**代码文件**
- `src/01_export_postgres_table.py` → 01_导出 Postgres 表（从 symbol 表拉全量数据到本地）
- `src/02_add_auto_columns.py` → 02_追加自动列（在导出表里补充派生/自动字段）
- `01_add_exchange_fee.sql` → 01_新增「交易所手续费」列
- `02_add_futures_trading_fee.sql` → 02_新增「期货交易手续费」列
- `03_drop_futures_trading_fee.sql` → 03_删除「期货交易手续费」列（前期误加，回退）
- `04_update_exchange_fee_verified.sql` → 04_更新交易所手续费（已与原始数据核对）
- `05_update_domestic_option_exchange_fee.sql` → 05_更新国内期权交易费（DCE/GFEX/ZCE/INE/SHFE/CFFEX）
- `06_update_cffex_index_option_fee.sql` → 06_更新中金所（CFFEX）股指期权费（HO/IO/MO）

**流程说明**
1. `01_export` 把 Postgres 的 `symbol` 全量导出到本地；
2. `02_add_auto_columns` 在本地表里补派生列；
3. 用手头的手续费 xlsx（data/ 下）核对后，用 `01~06` 一系列 SQL 把「交易所手续费 / 期货手续费 / 国内期权费 / 中金所股指期权费」回写进 `symbol.exchange_fee` 等列。

---

## 3. 外盘期货日线数据库（public.futures_kline_daily）

**代码文件**
- `01_migration_add_contract_month_date.sql` → 01_迁移_新增「合约月份日期」列（contract_month_date）
- `02_update_from_tradingview_2026-08-11.sql` → 02_TradingView 补日线（8-11 更新，318 行）
- `03_update_from_tradingview_2026-08-12.sql` → 03_TradingView 补日线（8-12 更新，93 行，MA 重算）
- `04_add_symbol_fk.sql` → 04_新增 symbol 外键（新增 symbol_id 列 + FK 约束 + 回填 10863 行）
- `decode_sample.py` → 解码样本（读源 SQL `D:\外盘数据\外盘期货日线数据库.sql`，揭示 exchange/exchange_code/variety 字段来源）
- `sample_output.csv` → 样本导出（源数据样例，GBK 编码）
- `月度合约字母编码说明.md` → 月度合约字母编码对照说明（如 1!=1 月、2!=2 月…）

**流程说明**
1. 源 SQL（`D:\外盘数据\外盘期货日线数据库.sql`）一次性建表，自带 `exchange/exchange_code/variety`；
2. `01_migration` 补 `contract_month_date` 列；
3. 根层 `update_foreign_kline_tv.py` 每日从 TradingView 拉新日线，生成 `02/03` 这类增量 SQL 入库；
4. `04_add_symbol_fk` 把品种/代号通过 `symbol_id` 外键关联到 `symbol` 表。

---

## 4. 套期保值hedging_announcements（public.hedging_announcements）

**代码文件（src/ 主流程）**
- `src/01_crawl_cninfo_hedging.py` → 01_爬取巨潮资讯套保公告（抓原始公告存 raw sqlite）
- `src/02_enrich_and_build_database.py` → 02_补全信息并建库（抽公司/行业/标题，推断套保品种 commodity；**含读 PDF 正文补品种**）
- `src/03_import_postgres_sql.py` → 03_导入 Postgres（把建好的库导出 SQL 写进 public.hedging_announcements）
- `src/04_incremental_update_postgres.py` → 04_增量更新 Postgres（按 announcement_id UPSERT 新公告）
- `src/07_backfill_commodity_from_pdf.py` → 07_从 PDF 回补品种（扫 commodity 空值，标题优先、正文兜底补抽）

**代码文件（sql/ 品种标准化与修正）**
- `sql/01_add_commodity_standardized.sql` → 01_新增「标准化品种」列（commodity_standardized）
- `sql/02_merge_standardized_into_commodity.sql` → 02_合并标准化品种到 commodity
- `sql/06_fix_dezhong_commodity.sql` → 06_修正德众汽车 commodity（补为「碳酸锂」）
- `sql/07_backfill_commodity_from_pdf_2026-08-11.sql` → 07_PDF 回补品种 SQL（8-11 批量）
- `sql/07_revert_dezhong_policy_2026-08-11.sql` → 07_回退德众管理制度错标（保持 NULL）

**流程说明**
1. `01_crawl` 爬巨潮 → 存 `data/raw_cninfo_announcements.sqlite`；
2. `02_enrich` 补全公司/行业/品种并建本地库；
3. `03_import` 全量导入 Postgres；
4. `04_incremental` 日常增量 UPSERT；
5. `07_backfill` 把历史空 commodity 用 PDF 正文补回；
6. `sql/` 下为品种标准化与个案修正（德众碳酸锂、管理制度回退等）。

---

## 5. 移仓换月宽表（public.移仓换月宽表）

**代码文件**
- `src/01_export_postgres_table.py` → 01_导出 Postgres 表（把移仓换月宽表导出到本地）
- `src/02_add_auto_columns.py` → 02_追加自动列（补派生/自动字段）

**流程说明**
1. `01_export` 从 Postgres 拉 `移仓换月宽表` 全量；
2. `02_add_auto_columns` 在本地补自动列，生成最终宽表。
